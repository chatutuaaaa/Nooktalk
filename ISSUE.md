# 社区功能规划（v1 产品 / 技术说明）

本文档汇总「登录、发帖、评论、点赞、PostgreSQL、角色与超管、后台、软删与 30 天硬删、禁言」等**已定稿**范围，供实现时拆 issue 与里程碑。  
**与当前仓库状态**：下述功能尚未在代码中实现，属路线图。

---

## 1. 产品范围（v1）


| 能力      | 说明                                                                                                         |
| ------- | ---------------------------------------------------------------------------------------------------------- |
| 浏览      | **未登录可浏览**帖子列表与详情。                                                                                         |
| 发帖 / 评论 | **仅登录**后可发帖、发评论。                                                                                           |
| 点赞      | 对**帖子**与**评论**均可点赞；**未登录不可点赞**；**登录**后可操作。                                                                 |
| 禁言      | 被禁言用户**不可发帖、不可评论**；**可以点赞、可以浏览**（与已登录只读+互动中的点赞一致）。                                                         |
| 删帖/删评   | **帖子、评论**均 **软删 + 可恢复 + 满 30 天未恢复则物理删**（见第 5 节级联与计时规则）。                                                    |
| 帖评级联    | **帖**被软删时，其下**所有评论**视为**随帖一起软删**；**帖**被恢复时，**随帖被软删的评论**与帖**一起恢复**；**帖**被**物理**删除时，其下**评论**及关联点赞**一并物理**删除。 |
| 角色      | **普通用户**与**超级管理员**；超管可删**任何人**的帖子/评论，普通用户仅能处理**自己**的。                                                      |
| 管理后台    | 超管可访问；展示**总用户、总帖子、总评论**等统计；**禁言/解封**普通用户；**删除/恢复**（与业务规则一致）。                                               |


---

## 2. 技术选型（建议）

- **数据库**：PostgreSQL。  
- **ORM / 迁移**：SQLAlchemy 2.x + Alembic。  
- **API**：REST、JSON；可与现有应用共存，新路由建议带版本前缀（如 `/api/v1`）。  
- **鉴权**：JWT（access + refresh）或 HttpOnly Session；**权限校验仅在服务端**生效。  
- **密码存储**：Argon2id 或 bcrypt（如通过 passlib）。  
- **管理端 UI**：与主站同仓库下独立路由（如 `/admin`）或后续再拆。  
- **角色模型**：RBAC 起步；`user` / `admin`（或 `is_superuser`），留扩展位（如 `moderator`）。

---

## 3. 数据模型（核心实体）

- **users**：登录标识（email 或 username 唯一）、password_hash、role、**禁言/封禁**相关字段（`is_silenced` / `silenced_until` / `reason` 等，按你命名统一）、`created_at` / `updated_at`。  
- **posts**：author_id、title、body、**软删**字段（`deleted_at` 等）。  
- **comments**：post_id、author_id、body、**自身**软删字段（`deleted_at` 等；与帖的级联见第 5 节）；可选 `parent_id`（楼中楼可二期）。实现上需能区分**用户/管理员对单条评论的软删**与**因帖子被软删而级联的隐藏**，以便恢复帖时仅恢复**级联**部分、单条已删的评可仍按原软删态（或按产品定为一并清空，**实现前锁一种**）。  
- **post_likes**：`(post_id, user_id)` 唯一。  
- **comment_likes**：`(comment_id, user_id)` 唯一。  
- **sessions / refresh_tokens**（若用 refresh）：`user_id`、jti 或 token 哈希、过期、吊销。  
- **admin_audit_log**（推荐）：超管**删帖/恢复/封禁/解封**等操作留痕。

**索引**：列表分页、外键、`likes` 唯一约束；软删列参与查询的复合索引视查询而定。

---

## 4. 权限矩阵（实现依赖必须覆盖）


| 行为            | 未登录 | 已登录 | 已禁言 | 超管           |
| ------------- | --- | --- | --- | ------------ |
| 看帖列表/详情       | ✅   | ✅   | ✅   | ✅            |
| 发帖            | ❌   | ✅   | ❌   | ✅            |
| 评论            | ❌   | ✅   | ❌   | ✅            |
| 点赞 / 取消赞      | ❌   | ✅   | ✅   | ✅            |
| 删自己帖子/评论      | —   | 依规则 | 依规则 | 可删**任意**用户内容 |
| 软删/恢复/后台统计/禁言 | —   | —   | —   | ✅            |


**说明**：禁言**只拦截「发言类」写接口**（发帖、评论及后续若有的编辑），**不拦截**点赞、读接口。

---

## 5. 软删、恢复与 30 天物理删（帖子 + 评论，已定稿）

### 5.1 共同规则（帖与评）

- **软删**：以 `deleted_at`（或等价）标记；**恢复**可清空或移动回收站态。  
- **公开读路径**：不展示**已软删**的帖与评（含「帖在、评因帖而级联隐藏」的展示规则见下）。  
- **作者**可恢复**自己**的帖/评；**超管**可恢复**任意**帖/评。  
- **30 天物理删**（**定时任务**）：对「仍处于软删且超过 30 天未恢复」的**帖子**与**评论**分别扫描；物理删时清理各自关联点赞等；具体 `WHERE` 以「软删时刻起算 30 天」为统一业务日。  
- 帖与评可**同一定时任务**内两段 `DELETE`（或事务），**运维上只配一个 job** 即可。

### 5.2 仅帖子级：删帖、恢复帖、硬删帖

- 帖子被 **软删** 时，该帖下**所有评论**在业务上**一并软删/隐藏**（实现上可：逐条写 `comment.deleted_at` + 级联原因，或「读评时若 `post.deleted_at` 非空则不可见」二选一，**以能支持「只恢复帖就拉回全部随帖被藏的评」为准** —— 与下条 **5.3** 一致时推荐能逐条或批量标记**级联软删**）。  
- 帖子被 **恢复** 时，**因该帖被删而**进入软删/隐藏态的**全部评论**与帖**一起恢复**（**一起恢复** = 与帖同批次级联藏起的那些）。  
- 帖子到点 **物理删除** 时：该帖下**评论、帖子点赞、评论与评论点赞**等 **一并物理删**，无孤儿行。

### 5.3 单条评论（帖尚未删时）

- 评论 **单独** 被作者/超管 **软删** 时，仍适用 **软删 + 恢复 + 30 天物理删**（与帖同一套**时间语义**）。  
- 若帖之后又被软删再恢复，**单条**已软删的评在「**恢复帖**」时**是否**仍保持软删：推荐 **是**（只恢复**随帖级联**的那部分），以免误把用户已自删的评又公开；**若**产品希望「一恢复帖则帖下**全部**可见」，实现时改为一并清空子评 `deleted_at`（**二选一，建议锁「级联/单条」模型**）。

### 5.4 展示与 404

- 帖或评软删后，列表/详情的 **404/「已删除」** 文案在帖与评上**统一**约定。  
- **点赞**：软删的帖/评侧栏通常不再允许对不可见内容点赞；若需「仍计历史赞数」由展示层决定，数据层在物理删时清点赞。

---

## 6. API 面（模块级，非最终路径）

- **Auth**：register、login、refresh、logout、me。  
- **Posts**：公开列表/详情；创建/更新/软删/恢复；作者与超管区别在服务端判定。  
- **Comments**：按帖分页、创建、删（自己或超管）。  
- **Likes**：帖子与评论上各自幂等的点赞/取消。  
- **Admin**：用户列表与禁言/解封、全站统计、**任意**帖/评的**删与恢复**、审计查询（若启用 audit 表）。

错误码、分页、DTO 在开工前在单独「接口约定」中补一页即可。

---

## 7. 安全与合规（底线）

- 生产环境 **HTTPS**；若 Cookie 会话，配 `Secure` / `HttpOnly` / `SameSite`。  
- **防爆破**：登录/注册/发贴接口限流。  
- **超管种子**：**禁止**在仓库硬编码；用环境变量或一次性初始化命令。  
- 日志、审计**不落明文密码、不落完整 JWT**。  
- 富文本或 Markdown 若开放，需 **XSS 白名单/消毒**（可列为二期若 v1 纯文本）。

---

## 8. 部署与已有架构（Nooktalk 现状衔接）

- 应用侧已存在 **Nginx 反代 + systemd + Uvicorn** 时，仅需增加 **PostgreSQL**（同机或托管 RDS）与 `DATABASE_URL` 等环境变量。  
- **部署步骤**需包含：迁移 `alembic upgrade`、**定时 job** 部署说明（30 天硬删）。  
- **备份**：`pg_dump` 或云厂商自动备份。

---

## 9. 建议里程碑（实现顺序）

1. PostgreSQL 接入、User、密码哈希、JWT、Alembic、基础依赖注入。
2. Post + Comment 的读（公开）与写（需登录 + 禁言检查）。
3. post_likes、comment_likes 与唯一约束。
4. 超管与 RBAC、软删/恢复、管理接口与**审计日志**。
5. 统计类 SQL 与管理后台 UI。
6. **定时任务**：30 天未恢复之**帖、评**的物理删，以及帖级联下的评论/点赞清理。
7. 限流、监控、备份与文档收尾。

---

## 10. 已定稿项回顾

- 未登录可看帖，登录才可发帖/评论。  
- 点赞覆盖**帖子 + 评论**；**禁言用户允许点赞**。  
- 禁言 = **不能发帖 + 不能评论**（不禁点赞、不禁浏览）。  
- **帖子、评论**均：软删 + 恢复 + **30 天**未恢复则**物理删**。  
- **帖**软删 → 其下**评****同样**按业务规则**随帖**软删/隐藏；**帖**恢复 → **因帖而级联**的那部分**评与帖一起恢复**；**帖**物理删 → **评**与关联数据**一并**物理删。  
- 单条评论独删、与「随帖级联」在数据模型中需**可区分**（见 §3/§5.3），方便恢复与计时。

**开工前可再定一条**（有默认建议）：**恢复帖**时，**帖仍正常时**已被**单独**软删的评论，是保持软删还是一并可见 —— `ISSUE` 中默认 **保持** 单条软删态。

---

*文档随实现进展可再迭代版本号与变更记录。*

---

## 11. 管理后台统计指标（新增建议）

为配合超管治理与运营决策，后台建议在现有「总用户 / 总帖子 / 总评论」基础上补充以下指标。

### 11.1 v1 必做（最小可用）

1. 今日 PV / UV。  
2. 今日新增用户数。  
3. 今日新增帖子数。  
4. 今日新增评论数。  
5. 今日点赞总数（帖子点赞 + 评论点赞）。  
6. 近 7 天活跃用户趋势（按日）。  
7. Top10 热帖（综合浏览、评论、点赞）。  
8. 当前禁言用户数 + 今日新增禁言数。

### 11.2 v2 推荐（增强运营视角）

- 页面维度 PV（首页、帖子详情、天气页等）。  
- 0 评论帖子占比（衡量内容冷启动与互动质量）。  
- 互动率：`(评论数 + 点赞数) / 浏览量`。  
- 次日留存 D1、7 日留存 D7。  
- 活跃用户分层（只浏览 / 只点赞 / 发帖评论）。  
- 治理数据：删帖/删评次数（普通用户 vs 超管）。  
- API 健康：4xx/5xx、平均响应时间、天气接口成功率。  
- 定时任务健康：30 天清理任务最近执行时间、成功/失败次数。

### 11.3 指标口径与展示约定

- 时间窗口统一支持：今日 / 近 7 天 / 近 30 天。  
- 统计口径默认不含已物理删除数据；对软删数据是否计入需单独标注（建议在卡片旁显示「含/不含软删」）。  
- 热帖榜建议提供权重配置（初始可固定）：`score = view*1 + comment*3 + like*2`。  
- 统计查询尽量走汇总表或缓存，避免直接扫大表影响主业务查询。